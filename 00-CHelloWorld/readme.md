# TP-0 C - Hola Mundo
## El compilador seleccionado
Estoy usando gcc que lo descargue con el Msys. Tuve que reinstalarlo ya que tenia una version del gcc anterior que la habia descargado antes con MingW. El problema era que el anterior compilador compilaba en la version de C de 2011 con el codigo: `201112L`. Como se pedía compilar en la version 2023. Averigue un poco (con IA) y me recomendo esta reinstalación.

## La versión del compilador
La versión del compilador ahora cambiado es: `gcc (Rev13, Built by MSYS2 project) 15.2.0`.

## La versión de C que el compilador compila
Finalmente. La versión de C que el compilador compila es la `202311L`.